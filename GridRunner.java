
public class GridRunner {

  private Grid grid;

  public GridRunner(int gridSize) {
    this.grid = new Grid(gridSize, gridSize);
  }

  //Initialize robots

  //Run Robots

  //Print Method

  //Determine when run is over


  public static void main (String[] args) {
        Grid grid = new Grid(9, 9);
        //Robots = Initialize Robits(Greedy, Straight, wait, Genetic);
        //RobitRun();
  }
}
